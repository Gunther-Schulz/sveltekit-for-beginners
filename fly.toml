# fly.toml file generated for sveltekit-for-beginners on 2022-10-24T11:38:16+02:00

app = "sveltekit-for-beginners"
kill_signal = "SIGINT"
kill_timeout = 5
processes = []

[env]

[experimental]
  allowed_public_ports = []
  auto_rollback = true

[[services]]
  # http_checks = []
  # internal_port = 8080
  # processes = ["app"]
  # protocol = "tcp"
  # script_checks = []
  # [services.concurrency]
  #   hard_limit = 25
  #   soft_limit = 20
  #   type = "connections"

  # provision ip: fly ips allocate-v4
  # deploy: fly deploy . --app sveltekit-for-beginners --image flyio/postgres:14
  internal_port = 5432 # Postgres instance
  protocol = "tcp"
  # Open port 10000 for plaintext connections.
  [[services.ports]]
    handlers = []
    port = 10000
  # [[services.ports]]
  #   force_https = true
  #   handlers = ["http"]
  #   port = 80

  # [[services.ports]]
  #   handlers = ["tls", "http"]
  #   port = 443

  [[services.tcp_checks]]
    grace_period = "1s"
    interval = "15s"
    restart_limit = 0
    timeout = "2s"
